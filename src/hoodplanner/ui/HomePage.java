package hoodplanner.ui;

import javax.swing.JFrame;

public class HomePage extends JFrame {
    public HomePage() {
        setTitle("Hood Planner");
        setSize(800, 600);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        setLocationRelativeTo(null);
    }
}
